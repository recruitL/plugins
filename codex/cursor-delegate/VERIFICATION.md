# 本轮验证（2026-09-17）

日常文件维护能力已在修复后的实际 Codex App 安装版本 `0.1.0+codex.20260917140653` 验证。插件保留安装；完成的执行会话已关闭。本轮没有重开历史安全探针或完整旧服务回归。

## 实际修复

此前非 execute 权限请求被直接拒绝。现在完整的原生 ACP read/edit 请求及 Cursor Read/Write hook 请求都进入现有 MCP 待审阅队列，由 Codex 检查路径和修改内容后逐次答复。

本机 Cursor `2026.09.15-d2fe57e` 的 ACP 不接入 `--plugin-dir` hooks；它从数据目录的项目 hooks 加载配置。适配器按实际路径合并审阅 hook，保留已有配置与备份，关闭会话时恢复。使用私有本地 socket，通信失败拒绝，不覆盖原生拒绝。

## 分层结果

| 层级 | 本轮实际结果 |
| --- | --- |
| 针对性单元及协议检查 | 13/13：文件操作分类、原生权限答复、实际 hook 子进程/本地 socket、拒绝与取消、原配置保留和恢复、原命令策略及打包 MCP 初始化。打包中的 CLI 是哨兵，不算真实 Cursor。 |
| 真实 Cursor 预检 | 修正 ACP 配置路径后，指定文件 Read 进入待审阅队列，Codex 答复后完成。此项为终端预检，不冒充 App。 |
| 新版实际 App | 启动回执确认上述安装版本和目标目录。读取、写入共 18 次经过 hook 审阅队列；不是依赖内部工具绕过桥接。 |
| 普通技术决策 | Cursor 提出转换/拷贝方案及技术问题，Codex 在同会话答复并推进，没有用户项目审批。 |
| 真实维护及返修 | 使用与插件无关的 Pallets itsdangerous 2.2.0 隔离副本；修改已有 encoding.py 与 test_encoding.py。Codex 首轮独立测试 18/18，发现整数和一般迭代对象被意外接受，要求同会话加类型检查。再修正非连续视图测试样例，最终 26/26，另有子类兼容检查 2/2。 |
| 有界故障处理 | 返修遇到一次 resource_exhausted；确认会话空闲且没有返修写入后，仅同会话重试一次并成功。没有更换计费配置或绕过拒绝。 |
| 收尾 | 执行会话关闭，临时项目 hook 配置恢复；插件仍安装启用。 |

## 具体未支持能力

- 可信人类安全升级通道尚未实现；需要新增授权的操作不能由模型声称批准。
- 60 KB 以上文件的内联审阅、删除和未知操作形式尚不支持。终端命令仍有原有支持范围，日常测试可由 Codex 执行。
- hook 覆盖本轮验证的 Read/Write 路径，不宣称覆盖所有内部工具或提供 OS 隔离。保留原生 sandbox 和拒绝行为。
- 原生问题/计划协议回调与意外断线恢复没有在本轮重测；本轮普通问答使用同会话文本。

历史验证见既有 tests/receipts 文件及 Git 历史，不将历史版本的结果计入本轮。
